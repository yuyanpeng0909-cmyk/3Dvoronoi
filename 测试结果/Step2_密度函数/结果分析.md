# Step 2 测试结果：密度函数 + 智能体初始化

## 测试结果汇总

**总计**: 7 PASS, 0 FAIL — **全部通过**

## 关键数值分析

| 测试项 | 数值 | 含义 | 是否符合预期 |
|--------|------|------|-------------|
| 密度最小值 | 0.1000 (=β) | 上游零浓度区的基底密度 | β>0 保证全域正密度 |
| 密度最大值 | 3.2139 | = α·C_norm_max + β | 源附近高密度 |
| 远处密度 | 0.1000 (=β) | 远离羽流区只有基底密度 | 低浓度退化为β |
| 公式验证 | 差值=0.00e+00 | 手动计算=函数计算 | α·C_norm+β 公式正确 |
| 智能体位置 | x∈[29,475] m | 8个AUV随机分布在域内 | 全部在 [0,500] 内 |
| 时变密度 | 1.51→1.64 | t=0→25s密度增加9.25% | 与羽流时变一致 |

## 密度场空间分布诊断

phi_delta (= φ - β) 的空间范围：
- 最小值: 0.000000，最大值: 9.547016
- phi_delta > 0.01 的点数: 10872 / 30000 (36.2%)

Y方向高斯剖面（x=150m处）：
| y (m) | phi_delta |
|-------|-----------|
| -200 | 0.000 |
| -100 | 0.000 |
| -20 | 0.574 |
| 0 | 1.425 |
| +20 | 0.574 |
| +100 | 0.000 |
| +200 | 0.000 |

X方向沿中心线：
| x (m) | phi_delta |
|-------|-----------|
| 55 (源附近) | 9.547 |
| 300 | 0.357 |
| 500 (远端) | 0.131 |

## 参数说明

- α/β = 10/0.1 = 100，属于"强聚集"模式
- 在源附近密度约为远处的32倍（3.21/0.10），驱动智能体明显向羽流聚集

## 生成图片

- `step2_density_visualization.png` — 包含3个子图的合并图：
  - 左：密度增量 φ-β 的X-Y切片（z=源深度），线性色标，展示锥形羽流结构
  - 中：密度增量的对数切片 log₁₀(φ-β+ε)，突出羽流边缘的精细结构
  - 右：密度分布直方图（α=10, β=0.10），大量采样点集中在β=0.1附近
